using templateGraph;

namespace Graph.Controllers {
    internal class DashedLine {
        private MainWindow mainWindow;

        public DashedLine(MainWindow mainWindow) {
            this.mainWindow = mainWindow;
        }
    }
}